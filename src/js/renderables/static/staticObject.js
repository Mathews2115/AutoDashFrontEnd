/**
 * @typedef StaticObject
 * @type {object}
 * @property {function} create - creates the renderable object
 * @property {function} refresh - used by theme changes
 *                                updates the textures/graphics of the renderable object
 */
